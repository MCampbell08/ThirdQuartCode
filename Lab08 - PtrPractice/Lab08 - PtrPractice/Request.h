#pragma once
enum class Request
    {
    equal, palindrome, compare, len, sub, reverse, lower, 
    copy, dup, reverse2, concat, lower2, vowels, test, quit, unknown,

    numRequests  // make sure this one is last
    };

